/**
 * Category interface
 *
 * Represents a single category used for navigation and filtering products.
 */
export interface Category {
    /** The name of the category (e.g., "all", "electronics", "clothing"). */
    name: string;
  }
  